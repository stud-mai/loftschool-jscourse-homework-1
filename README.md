#Домашнее задание №1

Функция `consoleRec` рекурсивно печатает содержимое массива в консоль. На вход она принимает всего git один аргумент - массив.
- Файл `recursion.js` - модуль с непосредственно самой функцией `consoleRec`
- Файл `test.js` - используется для тестов функции `consoleRec`

